/**
 * @file
 * Loads the qTip2 library.
 */

jQuery(document).ready(function(){
	jQuery('.text_field_tooltip_qTip2_formatter').qtip({
		content: 'I have a nice little callout pointer... nifty huh?'
  });
});